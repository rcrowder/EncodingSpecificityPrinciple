/* This is the module specification for the Introduce Delay Utility module.
 */

mod_name		Utility;
qualifier		Delay;
typedef struct {

	NameSpecifier	mode;
	Float	initialDelay;
	Float	finalDelay;

} Delay2;
