/* This is the module specification for the transpose channels module.
 * This module copies the input signal to the output signal, but sets the
 * amplitudes of specified input channels to zero in the output signal.
 */

mod_name		Utility;
qualifier		Transpose;
typedef struct {
	
	NameSpecifier	mode;

} Transpose;
